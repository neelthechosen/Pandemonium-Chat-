services:
  - type: web
    name: chaos-chat
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"  # or one of the other commands above
    plan: free
